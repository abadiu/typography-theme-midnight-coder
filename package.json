{
  "name": "typography-theme-midnight-coder",
  "description": "A theme for Typography.js.",
  "version": "0.0.4",
  "author": "Adrian Badiu <adrian.badiu@mail.com> (https://abadiu.xyz/)",
  "dependencies": {
    "gray-percentage": "^2.0.0",
    "typography-breakpoint-constants": "^0.16.19"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.0",
    "@babel/core": "^7.6.0",
    "@babel/preset-flow": "^7.0.0"
  },
  "keywords": [
    "css",
    "typography",
    "typography-theme"
  ],
  "license": "MIT",
  "main": "./dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist/",
    "flow": "flow",
    "lint": "eslint --ext .js,.jsx --ignore-path .gitignore .",
    "watch": "babel -w src --out-dir dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
